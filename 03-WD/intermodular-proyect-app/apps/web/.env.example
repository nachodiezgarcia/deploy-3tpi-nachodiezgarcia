# Empty in dev (Vite proxy handles /api -> backend). Set full URL in production.
PUBLIC_API_BASE_URL=

# Needed by Mentor IA client requests (compiled at build time in Vite).
VITE_OPENROUTER_API_KEY=
